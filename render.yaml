services:
  - type: web
    name: strategic-builders
    env: node
    plan: free
    buildCommand: npm install && npm run prisma:generate && npm run build
    startCommand: node dist/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false